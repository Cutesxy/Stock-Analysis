# 开发文档 (DEVELOPMENT.md)

> 本文档记录项目的架构、已实现功能、数据口径与开发约定。修改代码前请先读一遍"开发约定"。

## 一、项目定位

**双ETF量化作战系统**:面向单一用户的本地投资决策工具。核心思路:

- **压舱仓**(红利ETF 515180):高股息、通道低区买入、无限期持有,赚分红+回归。
- **进攻仓**(游戏ETF 159869):叙事资产、通道深区买入、持有约12个月,止损锚定上证指数而非板块自身价格。
- 一切买卖点由**月线36月滚动对数回归通道**(±1σ残差带)推导;一切胜率由**历史锚点/同构事件模拟**给出,不依赖预测。

这是一个研究工具,不是交易引擎:它帮你算"现在处在什么位置、规则说要做什么、历史上这么做的胜率是多少",下单仍由你在券商App完成。

## 二、架构

```
┌────────────────────── 前端 frontend/(APP式·解耦) ──────────────────────┐
│ index.html            APP外壳:顶栏+分页导航+视图容器+弹窗/通知根      │
│ css/style.css         深色主题(桌面顶栏导航/手机底部Tab,媒体查询)   │
│ manifest+icons        PWA清单与图标(可"添加到主屏幕")              │
│                                                                       │
│ js/app.js             启动器:加载→store→路由→轮询(唯一的编排层)    │
│ js/store.js           全局状态:单一数据源+订阅通知(解耦核心)      │
│ js/router.js          hash路由 #/dashboard #/positions #/quant ...  │
│ js/ui.js              通用UI:弹窗/通知/交易表单/动作行/σ仪表盘     │
│ js/api.js             fetch封装(含规则保存/重置)                    │
│ js/channel.js         通道实时重算(实时价替换当月收盘→重算最新期)  │
│ js/charts.js          纯SVG:通道图+作战位+十字线 / 收益曲线drawCurve│
│ js/ledger.js          流水引擎(持仓/加权成本/现金)+SQLite同步      │
│ js/config.js          常量与格式化                                 │
│                                                                       │
│ js/views/             四个视图模块(只依赖store,互相独立)           │
│   dashboard.js        概览:引导/待办/三张决策卡/三张图/时间窗口   │
│   positions.js        持仓:仓位卡/配置结构/收益曲线/流水维护      │
│   quant.js            量化:胜率三层/分区表/方案对比/追涨检验      │
│   settings.js         设置:规则参数编辑/数据维护/关于             │
└──────────────────────────────┬────────────────────────────────────────┘
                               │ HTTP(JSON)
┌──────────────────────── 后端 backend/ ────────────────────────┐
│ app.py        FastAPI:行情/数据/统计/流水/规则 5组API+静态托管   │
│ config.py     标的/规则/回测参数(默认值)                      │
│ storage.py    SQLite流水账(backend/data/ledger.db,不入库)      │
│ data/rules_override.json   用户自定义规则(不入库,运行时合并)  │
│ services/                                                     │
│   quotes.py     实时行情(腾讯qt.gtimg.cn)                    │
│   kline.py      复权K线(翻页抓取+内存/磁盘双缓存+断网回退)   │
│   channel.py    月线通道计算(与前端同口径)                   │
│   backtest.py   锚点模拟/分区胜率/同构事件/追涨检验(接受规则) │
└────────────────────────────────────────────────────────────────┘```

**解耦原则**:视图只从store读数据、不直接发请求;数据变更统一走`store.set()`→订阅者重渲染;弹窗/通知在`#modal-root`/`#toast-root`,不受视图重渲染影响。

**前后端分工原则**:
- 后端负责**重计算**(全量历史抓取、通道、回测)与**持久化**(SQLite流水);
- 前端负责**实时性**(30秒轮询行情、实时价重算最新通道期)与**全部渲染**;
- 数学口径在两侧保持一致(`channel.py` ↔ `channel.js`),修改任何一侧必须同步另一侧。

## 三、API 设计

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/quotes` | 实时行情(games/dividend/sse三标的) |
| GET | `/api/data` | 历史K线+月线+通道+规则+回测统计(进程缓存,TTL见config) |
| GET | `/api/stats` | 仅回测统计(`?refresh=1` 强制重抓K线重算) |
| GET | `/api/ledger` | 读取流水(SQLite) |
| PUT | `/api/ledger` | 整表替换保存流水(前端为唯一数据源,简单可靠) |
| POST | `/api/ledger/demo` | 写入演示数据 |
| GET | `/api/rules` | 当前生效规则(默认+用户覆盖合并) |
| PUT | `/api/rules` | 保存规则覆盖(白名单校验→写rules_override.json→失效缓存,回测按新规则重算) |
| DELETE | `/api/rules` | 恢复默认规则(删除覆盖文件) |

启动:`./run.sh`(或 macOS 双击 `启动.command`),默认 `http://localhost:8420`。

## 四、已实现功能清单

### 数据层
- [x] 腾讯复权K线翻页抓取(单页640根限制,自动向后翻页取全量)
- [x] 始终剔除当日bar(hfq当日未复权问题),实时价由行情接口补充
- [x] 内存+磁盘双缓存(`backend/data/kline_cache.json`),断网回退到最后一次成功数据
- [x] 实时行情解析(未加引号的响应格式兼容)

### 通道与规则
- [x] 月线36月滚动OLS(log价)+±1σ/−2σ残差带,滚动计算=无未来函数
- [x] 红利通道用后复权(含分红再投),展示按 spot/hfq 比率折回现价;游戏用前复权(无分红=现价)
- [x] 规则参数全部集中在 `config.py`(加仓档/止盈倍率/止损线/到期日)
- [x] 前端用实时价替换当月收盘后重算最新一期通道(盘中σ位置实时刷新)

### 回测(全部服务端实时计算,无硬编码数字)
- [x] 锚点组合模拟:沪深300月线通道≤−1σ月为锚点(2021-07起),每锚点持有12月,五种加仓方式对比(不加仓/只等回踩/突破追涨/立即加/混合)
- [x] E方案事件模拟:止损锚定上证120日低点,止盈=加权成本×1.135/×1.231(档位=持仓1/3),对比纯持有与严格止损
- [x] 分区胜率表:月线通道σ位置→12月前瞻收益,按区间统计
- [x] 追涨事件检验:低区20日新高事件,含"突破失败后仍胜率"与"追涨后最大回踩"
- [x] Clopper-Pearson精确置信区间(二分求根)

### 流水账(本地数据库)
- [x] SQLite持久化,买入/卖出/分红/入金四种流水
- [x] 持仓股数/移动加权成本/现金/已实现盈亏全自动计算
- [x] 动作行[记账]一键入账(价格/股数预填)
- [x] 止盈线随加权成本自动重算;止盈档位=当时持仓的1/3(100股取整)
- [x] 导出JSON备份/导入(跨设备迁移);演示数据一键载入

### APP化前端(v2)
- [x] 四页视图(概览/持仓/量化分析/设置)+hash路由,顶部Tab(桌面)/底部Tab(手机)
- [x] store单一数据源+订阅通知,视图互相独立(解耦,便于加页)
- [x] 首次使用引导(空流水时显示欢迎卡:演示数据或开始记录)
- [x] 弹窗式交易表单(替代内嵌表格,手机友好),通知反馈(toast)
- [x] 三张决策卡(实时价/σ位置仪表盘/动作清单/距离百分比/记账按钮)
- [x] 纯SVG图表:K线+通道带+作战位横线+成本线+实时脉冲点+悬停十字线提示(右侧标签自动碰撞避让)
- [x] 持仓页:仓位卡+浮盈+配置结构条(当前vs推荐)+**账户收益曲线**(流水逐日回放,净值vs累计入金)+流水维护
- [x] 量化页:胜率三层(系统/资产/情境P滑块)/分区胜率表/五方案对比/追涨检验与结论
- [x] 设置页:规则参数可视化编辑(保存→服务端白名单校验→回测实时重算)/历史数据重抓/演示数据/关于
- [x] PWA:manifest+图标,可"添加到主屏幕"当独立APP用;响应式布局(375px手机全适配)
- [x] 30秒行情轮询;交易时段识别;止损警报横幅;视图切换动画

## 五、关键口径(改动前必读)

1. **通道**:月收盘、36月窗口、log价OLS、残差σ。红利必须用后复权(否则分红会造成通道假破位)。
2. **锚点**:沪深300(非上证)月线通道≤−1σ,起点2021-07(数据可得的完整熊市)。中段=2021-07~2022-02,尾段=其余。
3. **E方案止损**:上证收盘<入场时回看的120日低点。板块自身价格不作为止损依据(该对照在事件回测中显著更差)。
4. **止盈档位**:当时持仓的1/3(而非固定股数)——仓位变化后档位自动缩放,与锚点模拟口径一致。
5. **同构事件**:120日低点反弹7~17%且仍在MA250下方,每5个交易日采样,避免集群。
6. **组合基准**(config.BACKTEST.portfolio):压舱4500/现金1500/进攻仓主仓900股+加仓池等值——改这里会改变全部组合层胜率数字。

## 六、开发约定

- **依赖最小化**:后端仅 fastapi/uvicorn/curl-cffi/pydantic;前端零依赖零构建(原生ES模块),任何人 clone 后无需 npm 即可运行。
- **数学口径单一来源**:通道/回测逻辑在 `backend/services/`,前端 `channel.js` 是其镜像;改动需双侧同步并重跑验证。
- **个人数据绝不入库**:`backend/data/`(SQLite流水+K线缓存)在 .gitignore;仓库里不出现任何真实持仓、成本、账号信息。
- **诚实原则**:所有胜率数字必须带样本量与置信区间;页面与文档必须标注"样本内、一轮熊市、不构成投资建议"。
- **中文注释**:面向中文用户的项目,代码注释与文档用中文。

## 七、验证方法

```bash
# 1. 后端单测式验证(直接调服务层)
.venv/bin/python -c "
from backend.services import kline, channel, backtest
from backend.config import SYMBOLS
kl = kline.get_klines(SYMBOLS, full=True)
gd = kl['games']
stats = backtest.compute_stats(gd, kl['sse'],
    channel.to_monthlies(kl['dividend']),
    channel.to_monthlies(kl['hs300']),
    channel.to_monthlies(gd))
print(stats['system'])"

# 2. 前端端到端(headless Chrome)
/Applications/Google\\ Chrome.app/Contents/MacOS/Google\\ Chrome \\
  --headless --disable-gpu --no-sandbox --virtual-time-budget=20000 \\
  --dump-dom http://localhost:8420/ | grep -c '<svg'   # 应=3
```

## 八、已知局限(诚实清单)

- 回测样本=一轮熊市(2021-2024),锚点n=18-19,所有胜率的95%CI都很宽
- 进攻仓标的2021-03上市,无2021年初样本(上市盲区)
- E方案假设"板块与指数破位同步"(2022年成立,其他时期未验证)
- 追涨检验n=8,极小样本,只有指示意义
- 前端"done标记"存localStorage(换浏览器丢失);流水存SQLite(换电脑需导出JSON)
- 行情接口非官方,可能随时变动;kline缓存TTL 12小时,重启服务生效

## 九、规则覆盖机制(v2新增)

设置页可编辑的规则参数(加仓价/股数、止盈倍率、止损线、到期日、压舱档位等)通过`PUT /api/rules`保存到`backend/data/rules_override.json`(gitignore,不入库)。后端在每次构建数据时执行**深合并**:`config.RULES`(默认) ← override(用户值),只覆盖已存在键的叶子。回测函数全程接受`rules`参数——**改规则即改回测**,胜率/模式对比/E方案全部按新参数实时重算,前后端展示与统计永远一致。

## 十、路线图(未实现)

- [ ] 多标的支持(标的列表改为配置化,而非硬编码两个)
- [ ] 回测参数化界面(在页面上调σ切点/止盈倍率,实时重算胜率)
- [ ] 周线级别图表与周收盘确认逻辑(当前追涨档确认需要人工看周K)
- [ ] 流水的CSV导入(从券商导出对账单)
- [ ] Docker部署(当前仅本机)
